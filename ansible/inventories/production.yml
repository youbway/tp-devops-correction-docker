---
all:
  vars:
    ansible_user: admin
    ansible_ssh_private_key_file: >-
      {{ lookup('ansible.builtin.env', 'SSH_PRIVATE_KEY_FILE') }}
  children:
    prod:
      hosts:
        production:
          ansible_host: "{{ lookup('ansible.builtin.env', 'SERVER_HOST') }}"
